


## NEW SYSTEM FOR BUILDER MODE

in the dashbard page, in the projects tab, the user can start a new projects(no project name of the user can be same) if connected to github, if not connected to github the user can use the app's supbase db1..db2..(make a separate required sql for this if not already), once started project then it creates the folder in the users connected github(if connected) or in the apps's user db1,db2 etc (if not), and then opens a new tab with new ui :


BUILDER MODE UI:


 in the left there will be a sidebar file explorer, where users can nagivate through the files and folders of the loaded/created project, and in the above of this sidebar there will be options to create file, create folders (icons only), and in the middle of the page there will be the viewer display for file codes, for image , for video(videopplayer), for audio (audio player), for 3d models (3d viewer), for code (code viewer with proper syntax hightlights etc), and in the right side there will be the inspector tab for inspecting the file properties of the selected files, like code files, image files, 3d models, etc and in the header: in the left top corner there will be the koye ai logo and the project name opened and undo and redo changes icon next to it, and in the right top corner there will be the play btn to run the project/scene


## some changes in the chatting interface:

in the top left side of the terminal writter txt add a connect project, which when clicked a pop up (blur the background elements), this pop up will show the projects of the user created and link the current chatting session to the project,
and then a top to bottom notification to know if import sucessfull

and if the user generates images, audios, videos, 3d models, codes etc, in the chatting interface when the chat session is connected to a project then it will save the generated files in the project folder.


## some changes in the dashboard page's profile tab:

in the media tab, where the generated assets are shown, in the assets display cards in the bottom right corner add a btn to import to project, and then if clicked on that the pop up will open to select the project to which to user wants to import the asset to, and then a top to bottom notification to know if import sucessfull


## in the project's tab, when the user clicks on a project:

now it does not open the babylon game engine but the BUILDER MODE UI