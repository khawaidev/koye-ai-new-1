

dont save the  users project asset in the localstorage item etc:

here for general assets gen: create a folder called general in the user conneted githb account, and then for the general asset generation the images of the users should be stored here (but here are the detailed steps whenever the user generates assets in the images gen , 3d model gen page, audio gen page,video gen page, manually it counts as general assets, and for these whenever an asset is generated then the apps db creates an asset it and then it is also sent accordingly with the actual asset in the githubs general folder , which will be later used for deletion, renames, import to project , specail conditions: whenever  a user imports an general asset into a project then the: it takes the asste's id form the general apps db and then the project id to which the usre wants to import the asset to and then , it sends a request to the github to copy  the aaset from general with {asset id it} with new asset id of its own now to the project {id} in the githubs projects folder of the user) it my full flow, the user creates a project in my platform, then my apps db stores the project name and the project id after created, and then the same project name with the same project id is created in the user's github acc, and here when the user creates a file in the project in my app, then the app db's generates an asset id with respect to project name , user id and project id, then it should also created it inside the project in the github with the asset id, project name , uid , and user id,[not the apps db, the apps db also stores project name, user id, project id, assets id(each files, assets created has its own di)], and also when the user deletes an asset in the project opened in my app, then the apps db deletes the asset id, sents the asset id to the tighub abd also deletes it, this happens same for rename of files 