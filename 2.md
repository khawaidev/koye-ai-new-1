

## new features in the inspector elements in the builder page, for the image type files:

in the builder page, if the user selects a image type file(.png,.jpeg etc) and views it, then in the bottom of the inspeactor element in the right, add two btns:  edit image(with a terminal style btn, as black txt with white bg, and black shadow and when hover then make them slight bigger and then release then return to original) , 

and generate 3d model(with a terminal style btn, as white txt with black bg, and white shadow, with black shasow borders and when hover then make them slight bigger and then release then return to original), each of this btns should be functional as :
FOR THE EDIT IMAGE BTN: when clicked on the edit image btn(the edit image btn should be turned into a btn with txt "close" which when clicked closes the input element), then an input prompt (same as from the chatting interfaces input elements) comes from the bottom to the lower bottom(stops at about 5%(it should stick to the bottom , without any of the input element getting overshadowed) of where it started, and then the user can type in the prompt to edit the image (currently opened and viewing in the builder page's file viewer),
for the system/working of the image edit with input as prompt + the currently viewing image, and output as iamge should follow:

here is the request code should look form (according to provider's documentation):
const url = 'https://gateway.pixazo.ai/byteplus/v1/getEditImage'; const headers = { 'Content-Type': 'application/json', 'Cache-Control': 'no-cache', 'Ocp-Apim-Subscription-Key': 'YOUR_SUBSCRIPTION_KEY' }; const data = { prompt: 'Make the cat eye blue', image: 'https://pub-582b7213209642b9b995c96c95a30381.r2.dev/byteplus/1757499948018-hntkjsg9kj.jpg', guidance_scale: 6, seed: 42 }; fetch(url, { method: 'POST', headers: headers, body: JSON.stringify(data) }) .then(response => response.json()) .then(data => console.log(data)) .catch(error => console.error('Error:', error));

(should use the same api as the image gen from koye-2dv1.5(pixazo api))

and should also have the loading state for this too, where the send messafge/sent arrow btn form the input element in the builder which appears after clicking on the edit image appears, here the send btn should be turned into a spinner animations icon, and should be disabled (not clickable) and when the image edit gen is done then bring back the send icon(remove the spinner) and make it clickable, here the edited image should be opened automatically in the viewer, also the previous image should be saved(the image the user used to edit) and the edit prompt(input element from below) should remain active unless closed, (close when the user clicks on the close btn), and for the newly edited image(for the inspectors element the edit image should be replaced by "close" btn as the edit image is already active)
(here the generate 3d model should alos be unclickable until the process is done.)


FOR THE GENERATE 3D MODEL BTN:


[here the loading state animation should be the generated 3d model btn should be turned into a {spinner animated icon} and a txt "loading"], and also the edit image btn unclickable while the 3d model is being generated.


when clicked ,1.check if the image which is the 3d model is to be generated frmo (the currently viewing image in the builder page) has a valid bg(valid bg if complete non gradient contrast to the image object), these following steps of checks should be made:[this process should also use the same loading spinner instead of the sent arrow icon as also stated above] 

✅ What You Want to Detect
✔ Check 1: Is the background uniform (not gradient)?
check 2: is the image objcect and the bacground have enough contrast?

following this process or similar(change if necessary):


[here implement a systme for node react tsx / for this project to do the checks]

CONDITIONS:

if background is gradient or does not have enough contrast between the iamge object and the image background: then show an alert/warning  pop up window to the user telling the problem, and ask whether the user wants to generate a clean background for the image to proceed to the 3d model generation?:

if yes then run this api process with the image:
const form = new FormData() form.append('image_file', photo) form.append('prompt', 'a cozy marble kitchen with wine glasses') fetch('https://clipdrop-api.co/replace-background/v1', { method: 'POST', headers: { 'x-api-key': YOUR_API_KEY, }, body: form, }) .then(response => response.arrayBuffer()) .then(buffer => { // buffer here is a binary representation of the returned image })

(# here the prompt should always be: make the background of this image clean non-gradient white or bacl depending on the image object's contrast, if the image object is dark then white background, if it's white then dark bacground)
[should use the VITE_CLIPDROP_API_KEY, 1, 2...   etc]

(should also use the spinner loading state as stated above)

if done then, a pop up window where it will show two images:

on the left the original image and on the right the image generated with the new changes baakground, and in between the middle an arrow btn poiting at the right image, and below two btns: cancel and proceed, if porceed then go to step 2, and if cancel then close the pop up windows, and also return to the non loading state of the inptut send element,

2.and then another pop up window where the user is asked to select the settings for th 3d model generated , like the resolutions , 512p, 1024p, 1536p, (1024 default) and texture or non-textured (default: textured) and along with a credit cost counter below which updates the credit cost for the generations according to the selected settings(changes instaly as soon as the setting is changes)[for the credit cost take a look at:
🧱 3D Models (koye 3d v1)
Basic (512)
20 credits (+5 credits if texture)[total 25]

Standard (1024)
50 credits (+10 credits if texture)[total 60]

High-Res (1536)
70 credits (+20 credits if texture)[total 90]
] 
and the btn to proceed and cancel:

if proceed: then proceed to 3,

if cancel: then close the pop up windows and return to the non loading state and clickable state of the edit image and the generated 3d moddel btns,

3.should trigger the 3d model generation(with the selected settings as from step 2)(api same used for koye-3dv1/hitem3d api) and should also use the spinner loading state as stated above(and the spinner animated icon should not be clickable while the 3d model is being generated), and also the edit image  btn should also be unclickable while the 3d model is being generated.

and if done then release the unclickable state of the btn's and also the generated 3d mode btn should return to its original state.
and then the new generated 3d mode file should appear in the assets/modelsfolder with a short name and the .glb extension