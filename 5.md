
## new flow for project craetion, connecting project to chat and opening project in the builder page:

1. the user is able to create project from the dashbaord's project page, and the chatting interface, throught the create project btn(greeting message)

NOTE:each and every assets, files (images, audios, videos, 3d models, files: like .js, etc etec) when after generated should have each unique id when stored in eachs respective bucket(should also match in the respective table)

2.and once the user creats a project then, a separate project's table should be created in  db1 (along with the project's id, user's id, and also the bucket for storing the assets of that project, this bucket should have the same id as the project's id (here in this bucket the assets/files created/uploaded should have their own unique id)), (if full already exists then db2, db3 etc.).

3. CLEAR INSTRUCTIONS: there should be two types of assets generated,

they are: 1.general : which are generated assets when there's no connected project (these assets are not stored in the user's spcific projects, and these are the assets appearing in the profile page of the assets tab,)(it should be stored in the user's general assets table with unique id's and also a separate bucket for it)


2.project specific : which are generated assets when there's a connected project (these assets are stored in the user's spcific projects, and these are the assets appearing in the builder's page (the builder page sidebar filesystem)) and they should also appear in the profile page of the assets tab (as that is the universal assets tab)

## conditions/satuations:
1.when an asset from the general category is imported to a project then: (should copy form the general assets table and paste it in the project's assets table (generate new unique id fo the imported asset and also copy the asset from the general assets bucket to the project's assets bucket (the asset should be stored in the project's assets bucket with the same name as the general asset))) and should ultimately appear in the builder's page filesystem for the connected project,

2.when an asset from any of the category is deleted then it should be permanantly deleted form the respective db too(should delete by id's of the assets, as names of the assets can be renamed.)

3.when an asset form any of the category is renamed then it should be renamed in the respective db too(should rename by id's of the assets, as names of the assets can be renamed.)

4.when an asset from any of the category is being sent to another api endpoint (as public file url) then it should sent form the respective db's and bucket's, like for edit image in the builder page, where the user selected an image and then sents it to another api endpoint that accepts image as urls for iamge edting, here it should use the same id of the image selected in the builder page and then find the same id in the project's table and projects bucket and then sent the image to the api endpoint as public file url. etc

