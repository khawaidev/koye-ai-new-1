

## ai thikning animation in the chat interface changes:

in the chatting interface, instead of the ai logo/icon be just the txt ai let it be the icon.jpg , which should have three states:
1.idle animation state(in here the animation should be the gentle loop spin of the icon), (should change to thinking animation after the user sends a request/message)

2.thinking response state:(here the animation shoul be the icon gentle spin with the same phase and after a 360 roate then another animation which is the icon shirnks in to 80% of its size and the retunrs to normal size (with animation ) and then spins again in the same gentle pace and then loops )(and should return to idle animation if user stops it or if the response is printing then quickly load the printing response animations)

3.printing response animation:(in here the animation should be  the icon spinining faster, and loops)( and the animation should return to idle animation if it stops/complete printing the response)