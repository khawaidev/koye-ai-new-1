## ADDING TOOLS SYSTEM FOR THE LLM IN THE CHATTING INTERFACE TO USE IT TO INTERACT WITH THE FILES AND FOLDERS OF THE USERS CONNECTED PROJECT:


1.update the prompt for the llm in gemini.ts to check the right tools to use for the users request[if it reqiures any tool in the first place]

HERE ARE THE TOOLS FOR THE LLM/AGENT TO USE:

PROJECT FILE/FOLDER SYSTEM TOOLS:

1.create_file/create_folder[it can create files or folders in the project]
2.delete_file/delete_folder[it can delete files or folders in the project]
3.get_file_contents[it can get the contents of the files in the project]
4.edit_file[here the llm can choose the lines to lines to which it wants to edit/change or delete etc]
5.list_files_folders[it can list the files or folders in the project or folder inside the project]
6.rename_file/rename_folder[it can rename files or folders in the project]
7.move_file/move_folder[it can move files or folders in the project]
8.copy_file/copy_folder[it can copy files or folders in the project]
9.paste_file/paste_folder[it can paste files or folders in the project]
10.cut_file/cut_folder[it can cut files or folders in the project]
11.paste_file/paste_folder[it can paste files or folders in the project]
12.cut_file/cut_folder[it can cut files or folders in the project]
13.search_codebase[it can search the codebase for any file or folder or text]
14.replace_code

#NOTE: since the actual user project files/folders are stored in the r2, for the above tool usage and for changing the files/folders of the user project, should use some sort of cloudflare r2 tools to interact with the files/folders api, and also should use some sort of sandbox, and the user can accept or reject the changes before applying them to the actual user project files/folders, but can actually see the result of the edited file in the sandbox even without accepting first.

#NOTE: THE ABOVE FILE/FOLDER TOOLS REQUIES USER PERMISSION FOR EACH AND EVERY ACTION[in the chatting interface should show a banner like {let agent use{type of tool } {x and tick icons for reject or approve}{here an dropdown with two options, ask everytime and execute everytime(default is ask everytime, if selected the execute everytime , then in the next file/folder tool call dont have to ask for user permission to execute the command)}}]

#NOTE: THE ABOVE EDITED/CHANGED/DELETED/RENAMED/MOVED/COPIED/PASTED/CUT/SEARCHED/REPLACED/CREATED FILES/FOLDERS SHOULD BE VISIBLE IN THE SANDBOX IMMEDIATELY AFTER THE TOOL IS CALLED, EVEN WITHOUT THE USER ACCEPTING THE CHANGES, BUT THE ACTUAL USER PROJECT FILES/FOLDERS SHOULD ONLY BE CHANGED AFTER THE USER ACCEPTS THE CHANGES.AND THE REJECT/ACCEPT UI SHOULD BE LIKE THIS: [{FILENAME } {NUMBER OF LINES OF CODE ADDED AND REMOVE, WITH GREEN NUMBER BEING LINES ADDED, RED NUMBER BEING LINES REMOVED}{small red x icon to reject and small green tick icon to accept}]

ASSET GENERATION TOOLS:

1.text-to-image
2.image-image
3.tts generation
4.sfx/soundeffects gen
5.music gen
6.text-to-video
7.image-to-video
8.video-to-video
9.text-to-3dmodel
10.single-image-to-3dmodel
11.4images-to-3dmodel
12.auto-rigging
13.animation gen

#NOTE THESE ABOVE TOOLS SHOULD BE CALLED BY THE LLM ONLY WHEN THE USER GIVES PERMISSION TO USE THEM by this exmaple:

#NOTE ALL THESE ABOVE GENERATIONS HAVE GENERATIONS SETTINGS THAT THE USER CAN CHANGE AND DEFAULTS VALUES FOR EACH, OR THAT CHECK the mediagen.tsx, audiogen.tsx, 3dmode gen codes

user indicates he/she wants to generate an asset then -> the user chats with the llm to refine the asset idea -> once the ai gathers/knows enough/more of the asset the user wants then -> the ai should ask the user for permission to use the asset generation tool[in the form of a task card, a horizontally elongated rectangular card like: {tool name} {tool generation settings like model selections, ratio selections, duration selections,file selection}{cost of the generation by credits(should update dynimically with the live changes in the generation settings(no credit cost is well defined for each generation so, just show demo cost etc))} {reject and accept buttons}]

IF THE USER ACCEPTS THE ABOVE:
   IF THE TASK TYPE IS IMAGE GEN[TEXT-TO-IMAGE OR IMAGE-TO-IMAGE]:
      1.in the chatting interface, the llm thinking animation should be replace by a spinner element spinning, with the text Generating image.. and below this elements, there should be a semi big empty image container[with the same image ratio as selected in the generation settings], with a blurry gradient  animation from top left to the bottom right, like shimmering animation, and once the image generation in sucefull then replace the image loading animation with the actual image generated, and if error in generation then show the error message in the place of the image container, and the error message should be like: {error message} {try again button}, which when clicked try again then, request the same with the same generation settings etc.
      and then when sucesfull when the user hovers over the image container element then a download btn should appear in the bottom right corner.



    IF THE TASK TYPE IS AUDIO GEN[TTS, SOUND-EFFECTS AND MUSIC GEN]:
      1.in the chatting interface, the llm thinking animation should be replaced by this loading icon:

      npx shadcn@latest add @animate-ui/icons-audio-lines
      <AudioLines animateOnHover />
      // Or use with the AnimateIcon component
      <AnimateIcon animateOnHover>
        <AudioLines />
      </AnimateIcon>
      
      (should play the icon animation in loop till the generation is done)
      with text Generating Audio..
      IF THE TASK IS DONE THEN:
      replce the icon animation and the generting text with an audio player element.
      and if error in generation then show the error message in the place of the image container, and the error message should be like: {error message} {try again button}, which when clicked try again then, request the same with the same generation settings etc.
      and then when sucesfull in  the audio player element then a download btn icon should appear in the right corner of the audio player element.


    IF THE TASK IS VIDEO GEN[TEXT-TO-VIDEO OR IMAGE-TO-VIDEO OR VIDEO-TO-VIDEO]:
      1.in the chatting interface, the llm thinking animation should be replaced by this loading icon:

      npx shadcn@latest add @animate-ui/icons-clapperboard
      <Clapperboard animateOnHover />
      // Or use with the AnimateIcon component
      <AnimateIcon animateOnHover>
        <Clapperboard />
      </AnimateIcon>

      (should play the icon animation in loop till the generation is done)
      with text Generating Video..
      and below this elements:
      a video container with the same bg blurry animations as the image gen loading, with the same ratio as the select in the settings[for text to video and iamge to video] and for video to video use the ratio of the imported video ref for the edit.
      IF THE TASK IS DONE THEN:
      pause the animation icon, replce the generating video txt with Video generated txt, and replace the video container laoding with the actual generated video with a video player controls[play, pause, volume, fullscreen,etc], when hover on the video container element after the generation is sucessful then a download btn.if error in gen then show the error mesg(normal user redable) and a retry btn icon(should be functional)

    IF THE TASK IS 3D MODEL GEN[TEXT-TO-3DMODEL OR SINGLE-IMAGE-TO-3DMODEL OR 4IMAGES-TO-3DMODEL]:
      1.in the chatting interface, the llm thinking animation should be replaced by this loading icon:

      of a bone animated icon[should be white in dark theme of the page and viec versa]

      (should play the icon animation in loop till the generation is done)
      with text Generating 3D Model..
      IF THE TASK IS DONE THEN:
      pause the animation icon, replce the generating 3d model txt with 3D Model generated txt, and then in the side of it add a view in editor btn [which when clicked on open the 3dmodelgen's viewer with the generated 3d model opened in the viewer]and a download btn icon.if error in gen then show the error mesg(normal user redable) and a retry btn icon(should be functional)
      

AGENT LLM CONTROL TOOL[tool which can the agent call at its will without the user permission]:

1.search-web-api[the agent should call this tool when it has no context/idea about what the user is referring to]



